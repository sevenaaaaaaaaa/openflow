# OpenFlow v2.0

> 基于 TIPS，同时运行 Flow 与 Loop 的开源增长操作系统。跨形态：Web / PWA / CLI / MCP。

## 形态 · 一套后端多端触达

- **Web 后台**：`/xmp/` 全套管理（数十功能页）
- **PWA 可安装**：mac / win / linux / iPad / 安卓pad / 鸿蒙 —— 浏览器「添加到主屏幕/程序坞」即装成应用，零套壳
- **CLI**：`php bin/of <命令> --json`（agent 与脚本友好）
- **MCP**：`mcp-server.php`，18 只读 + 5 动作工具，`McpGuard` 逐工具鉴权/审计

## v2.0 核心能力

- **画布真流程图**：SVG 贝塞尔连线 + Pointer 拖拽 + A/B 分流 + 灰度放量 + 多条件分支 + 真延时续流（此前是横向列表 + 中断 delay）
- **模块工厂（Elementor 式）**：后台可视化定义新模块（字段/样式/代码模式 `{{field}}`），无需写 PHP
- **数据洞察**：驾驶舱 KPI（delta/月目标）+ 趋势/归因图 + **AI 兜底解读**（异常/建议）；报表不是只展示
- **CDP**：身份图谱迁 SQLite、深分群/自动打标/画像/生命周期引擎（默认规则开箱即用）、第三方数据源导入（Segment/GA4/神策）
- **CRM**：线索 360° 详情（编辑/跟进/待办/画像/标签/订单/行为时间线）、管道/看板/客户/原始导入
- **营销自动化**：触发器 → 多渠道动作（邮件/企微/公众号/券）+ 分工作流漏斗洞察 + A/B + 灰度
- **Newsletter（Revue 式）**：选文章自动排版 → contenteditable 可视化编辑 → 实时预览 → 定时发送
- **邮件**：SMTP（本机/外部）+ BillionMail/Mautic 渠道，退订/打开/点击统计，送信已验证
- **Agent 渗透**：dashboard/cdp/insights 核心页 AI 解读、问数据（AskData）、下一最佳动作（NBA）、决策轨道
- **编辑器所见即所得**：页面块级实时预览 + 整页预览；文章已 contenteditable
- **SEO 全局兜底**：canonical/description/OG 自动补，修 example.com 域名 bug
- **前台快速编辑**：登录后页角「编辑此页」直达后台（按 URL 映射）

## 技术栈

PHP 8 + JSON/SQLite，**零构建链**（无 Node/Composer 依赖），原生 JS + SVG（PWA/画布均原生实现，未引入 Electron/Tauri/React）。

## 已知限制（v2.0 如实标注）

- **短信**：演示模式（仅记录），未接供应商 API；注册验证码仅邮箱
- **社媒发布**：公众号/邮件真发，其余 8 平台为「生成文案手动发布」
- **广告投放**：ROI 台账（手动录入），未接主流广告 API 自动投放
- **App/H5/小程序/客户端推送**：缺失（仅站内微信/IM webhook）
- **报表**：固定指标预制，无自定义维度/钻取（后续）
- **邮件主编辑器**：邮件中心为裸 textarea（可视化在 Newsletter 编辑器，后续合并）
- **自治 Loop**：沙盘只读演示，不写生产（安全限制）
- **Copilot**：模板化意图识别，非真 NL 编排

## 此前已交付（延续）

内容/SEO/CDP/MA/CRM/电商/课程/社区/插件生态全链条审计落地（T0-T2 施工图 36 条），93 契约测试 + 44 页渲染冒烟全绿。

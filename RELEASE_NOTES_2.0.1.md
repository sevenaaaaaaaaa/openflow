# OpenFlow v2.0.1

> 增量版本：v2.0 基础上完成**功能健康盘点（A类死功能可用化 + B类浅功能去误导）+ AI 渗透编辑流程 + 视觉优化**。

## 本次（v2.0.1）新增

### 死功能可用化（盘点 A 类——"挂了没效"→真正生效）
- **301 重定向**（redirects.json）：请求期拦截真正 301（此前只存不执行）
- **SEO 管理**（seo.json）：公共页头优先读后台配置（此前改了对前台零生效）
- **SEO 站点 meta**（seo-settings）：og_image/robots/谷歌百度验证真正输出
- **结构化数据**（structured/*）：接进 json_ld（此前存了不渲染）
- **一键复用**（BlockModel repurpose）：文章 → 提纲/社媒/邮件/口播脚本（此前只有单测）
- **模块工厂**：补侧栏入口（此前仅 Studio 可达）

### 浅功能去误导（盘点 B 类——"看着能实则空"→如实标注）
- **邮件主路径**：引导到可视化 Newsletter 编辑器（裸模板降级）
- **社媒发布**：非真发平台改 `ok=false + manual:"仅生成文案需手动"`（不再假成功）
- **短信**：status→demo（演示仅记录）
- **广告**：改「投放计划台账」，去"自动优化/CAPI"误导
- **报表**：标"预设固定指标，自定义维度后续"

### AI 渗透编辑流程（Agent 渗透到每一步）
- **文章编辑器**：润色/续写/生成标题/摘要（`AiCenter::chat` + 鉴权防公开烧额度）
- **表单编辑器**：描述 → AI 生成表单字段
- **画布编辑器**：描述 → AI 生成流程节点
- **文章一键复用**：确定性生成多平台内容
- **表单条件逻辑**（GAP P1-5）：`show_if` 字段条件显隐（线索资格预筛）

### 视觉
- **AI 小助手**：新增「花嫁」二次元主题（粉白配色）+ 默认头像改为二次元花嫁女孩 SVG（金发+头纱+婚纱+花朵）

## 已知限制（同 v2.0，已列入 Roadmap）
- B4 App/H5/小程序/客户端推送：缺失（需外部服务）
- B7 Copilot 真 LLM 编排：当前为模板化意图识别

## 技术栈
PHP 8 + JSON/SQLite，零构建链（原生 JS + SVG），PWA/画布/小助手均原生实现。

# OpenFlow v2.5

> 主题：从「功能堆叠的系统」走向**自生长、AI-native 的增长操作系统**——
> 把 9 点深度体检逐条闭合成"能跑、能联动、能学习"的业务链路，并追平 CDP / SEO / GEO / Email / 直播 的专家基准。
> 交付原则：**加法优先、不动现有内容、不留假功能**；每一步都有契约测试与线上验证。

---

## 一、核心范式：一条「行动脊柱」

| 能力 | 说明 |
|---|---|
| **统一「人」对象** | CDP 画像 + CRM 线索 + 会员 + 订单 + 学习 + 互动 合并成一个人 + **一条跨模块时间线**（`/xmp/person`） |
| **今日主线** | 全站信号收口为 **立即 / 今日 / 本周** 三泳道行动队列；完成/稍后回流 |
| **小福判断层** | 基于**真实业务快照**推理出「今天先做这件事 + 理由 + 可执行计划」，执行留回执 |
| **一句话指挥** | 控制台自然语言指令 → 可执行计划 → 白名单校验 → 审批执行 |
| **自动装配（压轴）** | 投喂（文本/文件/站点·RSS）→ 按 **TIPS + 用户旅程**生成一整套就位项（flow/目标/分群/内容/落地页/待办）→ 逐个点确认，**接受即真实创建** |

## 二、9 点体检逐条闭环

1. **课程 ↔ 直播**：直播课成为一等课时；房间绑定课程课时；课内直播/回放/章节；出勤进学习进度。
2. **课程体系深项**：类型单一真源；**先修/学习路径**；**结业证书**（完课自动颁发、可验证）；学员进度与课程分析（完课率/课时流失）；批量操作。
3. **活动体系化**：票种 / 签到核销 / 议程 / 统计与名单导出；**报名即联动 CRM+CDP+自动化**（不再孤岛）。
4. **线上转化体系化**：修复 CAPI 调度 / purchase 回传 / 漏斗定义 / 归因写入死链；新增**转化目标对象**（事件+作用域+价值）并绑定落地页 CTA 追踪。
5. **模块工厂**：分类树 + 样式变体 + 构建器分类面板/搜索 + 克隆/导入导出；**组合模板**（成套区块一次插入，内置 4 套业务模板）。
6. **交互现代化**：控制台判断/回执、对话式 BI、自定义报表下钻、实时人群看板——关键动作就地完成、不整页刷新。
7. **Flow / Loop 自生长**：决策轨迹闭环 + **结果学习加权**（有效上浮/无效下沉）+ 控制台结果评估回流；Flow 增补**内容/销售/电商原子节点**（更新线索/建任务/分群/发布内容）。
8. **插件生态**：权限**运行时强制** + 扩展面（区块/CLI/MCP 注册口）+ 依赖检查 + `plugin new` 脚手架 + 回滚 UI + **AI 生成插件纳入安全审查**。
9. **主线**：见第一节（行动脊柱 + 控制台）。

## 三、专家基准追平

- **CDP**：分群即服务 API（令牌拉取分群/成员）；**标签表达式工厂**（嵌套 AND/OR + 路径/运算符）；**实时人群看板**（在线/新增/分群实时人数）。
- **SEO**：**内容健康分**（逐项可解释）+ **内链引擎**（识别未链接提及、一键插链）+ hreflang x-default；**软 404 处理**（筛选/空页 noindex+canonical）+ `/wp-*` 统一 410。
- **GEO**：**实体图**（品牌/产品/人物/话题 + 共现关系）；**竞品引用分析**（share of voice）；**可引用格式改写**（结论/关键事实/Q&A/表 + FAQ 结构化）。
- **Email**：**送达率中心**（SPF/DKIM/DMARC/MX 自检、抑制名单、退信/投诉率）+ **邮件收入归因**。
- **直播互动**：抽奖、**秒杀库存（原子扣减防超卖）**、**实时成交看板**、回放切片、连麦管理。
- **系统体检**：体检**历史与趋势** + 定时任务/存储检查 + 自优化建议并入。

## 四、趋势与运行时（AI 时代能力）

- **热点雷达**：HN / GitHub / RSS 多源抓取 → 去重 → AI 聚类热度 → 一键转 GEO 选题。
- **对话式 BI**：全站经营数据，自然语言问 → 选数据集 → 原生 SVG 出图 → 追问 → 下钻。
- **自定义报表**：任意「维度 × 指标 × 筛选」聚合 + 逐行下钻 + 保存复用。
- **Agent 运行时**：给目标，Agent 在**白名单工具**里自主链式执行；**重试自愈** + **风险审批门**（safe 自动 / 中高风险待批准）。
- **全平台发布框架**：适配器注册表（通用 Webhook / Telegram / Discord / Mastodon / WordPress + 公众号/邮件）+ 手动平台诚实降级 + **统一发布队列与重试**。

## 五、交易与基础修复

- **交易收据**：支付成功自动出票（幂等）+ 邮件发送 + 可打印页。
- **多支付渠道**：Stripe / 微信 Native / 支付宝 RSA2 真构造 + 回调验签；市场定价/交付接线；创作台闭环。
- **Copilot 真 LLM 编排**：模板快速路径 + LLM 兜底 + **白名单安全边界**。
- **404 兜底体系**：`ErrorDocument → 404.php`（让 redirects 对 Apache 级 404 也生效）+ 410 支持 + `no-store`；**404 分诊工具**（GSC/日志清单 → 301/410/待审）。
- **demo 残留清理**：早期 demo 内容/品牌/专题一键清查（本地 + 服务器，幂等可回滚）。

## 六、质量

- 契约套件 **125 passed · 0 failed**；render_smoke **60 页**；admin_contract **1230**；ai_resilience 22。
- 全程 `git push` + `bin/deploy.sh --live`，本地 / GitHub / 服务器三端一致。

## 七、已知限制（诚实标注）

- **直播收流 / 连麦媒体层**：需独立流媒体服务器（当前为运营层，媒体传输依赖外部服务）。
- **全平台发布**：框架与真发连接器已就绪，**真发需各平台凭据**（在「内容分发」面板填写并测试）。
- **短信真发**：演示模式，需接供应商。
- **App/H5/小程序/客户端推送**：需外部服务（JPush/APNs/FCM）。
- **百度搜索 API**：占位实现。
- **多租户 / 高可用**：按需，单站点优先。

## 技术栈
PHP 8 + JSON/SQLite，零构建链（原生 JS + SVG），PWA / 画布 / 小助手 / 图表均原生实现。

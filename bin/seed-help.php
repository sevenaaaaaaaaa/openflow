<?php
/**
 * seed-help.php — 为帮助中心补充「OpenFlow 自知识」文章
 *
 * 背景:帮助中心已有 20 篇(h001-h020),但 Flow 画布 / SEO 关键词 / GEO / 热点雷达 /
 *       Agent 运行时 / 转化目标 / 运维清单 / 全平台发布 / 定价方案等支柱主题缺位。
 *       用 OpenFlow 讲清楚 OpenFlow 自己,是产品可信度的直接证据。
 *
 * 用法:
 *   php bin/seed-help.php             # 应用(自动备份 help-center.json)
 *   php bin/seed-help.php --dry-run   # 预览
 * 幂等:按 slug 判重,已有文章一律跳过;服务器与本地均可安全执行。
 */
require_once dirname(__DIR__) . '/admin/config.php';

$DRY = in_array('--dry-run', $argv, true);
$file = DATA_DIR . '/help-center.json';
if (!is_file($file)) { echo "未找到 {$file}(帮助中心数据在该环境不存在),跳过。\n"; exit(0); }

$TODAY = date('Y-m-d');
$NEW = [
  ['h021', 'pricing-plans', 'account', '定价与方案：开源、云托管、私有化怎么选', '核心能力全部开源免费；商业版卖的是托管与服务,不锁功能。一张表看清四个方案。', true, 1, <<<MD
> 一句话原则:**功能不锁,按服务付费**。开源版与付费版的能力完全一致,区别只在谁来运维、支持多快、部署在哪里。

## 四个方案

| 方案 | 价格 | 适合 |
|---|---|---|
| 开源自部署 | ¥0 永久 | 动手能力强、数据要握在自己手里 |
| 云托管 · 个人版 | ¥99/月 | 一个人的公司,时间比钱贵 |
| 云托管 · 增长版 | ¥299/月 | 2-5 人小团队,要席位与权限 |
| 私有化 / 定制 | 报价后详谈 | 有合规、SSO、SLA 要求的企业 |

## 三个常见问题

1. **以后能互相迁移吗?** 能。数据结构同源,导出/导入工具齐全,双向迁移。
2. **AI 要额外花钱吗?** 开源版接你自己的模型 Key,费用直接付给模型厂商;云托管含月度额度,后台透明可查。
3. **能退款吗?** 云托管 7 天无理由退款(年付按月折算)。

完整对比见前台 `/pricing`,企业方案见 `/enterprise`。
MD],
  ['h022', 'flow-canvas-first-automation', 'growth', 'Flow 画布：画出你的第一条自动化流程', '从「询价表单进 CRM + 超时未跟进自动提醒」开始,十分钟拖出第一条稳定执行的增长流程。', true, 2, <<<MD
> Flow 的价值是**把你的 SOP 变成系统稳定执行的动作**,AI 只辅助设计,执行完全确定。

## 第一条流程:询价自动跟进

1. 进入 `后台 → Studio 模式 → Flow 画布`,新建画布。
2. 拖入触发器「表单提交」,选择你的询价表单。
3. 加条件分支「客户类型 = 企业 / 个人」。
4. 企业分支:动作「创建 CRM 线索」+「创建跟进任务(负责人:销售)」。
5. 加延时节点「等待 48 小时」,再接条件「线索仍未跟进」→ 动作「发提醒给负责人」。
6. 保存 → 点「启用」。每一步执行都有留痕,画布上能看到实时流转。

## 预设模板

不想从零开始,面板里有四套业务模板(内容获客 / 线索培育 / 直播带货 / 复购召回),一键插入后改成自己的话术即可。

## 边界提醒

高风险动作(群发、改价、对外发布)节点默认需要人工确认,画布里会明确标出。确定性流程 + 人工闸门,是 Flow 与「AI 随便跑」的根本区别。
MD],
  ['h023', 'seo-keyword-layout', 'growth', 'SEO 关键词布局：健康分、内链引擎与四搜索引擎', '用内容健康分逐项打分、内链引擎一键补链,把关键词布局做成可执行清单而不是玄学。', false, 3, <<<MD
> OpenFlow 的 SEO 体系分三层:**写之前**有关键词与话题,**写完有**健康分体检,**发布后有**收录推送与内链。

## 内容健康分

`后台 → SEO 中心 → 内容健康分`:每篇文章给出可解释的逐项得分(标题长度、关键词出现位置、内链数量、结构化数据、meta 完整性)。低于阈值的文章会列出具体改进项,照单修改即可。

## 内链引擎

`SEO 中心 → 内链引擎`会扫描全站文章,找出「提到了某篇已有文章的主题但没挂链接」的位置,支持一键插入内链。老文章的历史价值就是这样被激活的。

## 关键词布局三步

1. **选题层**:热点雷达与 GEO 采集产出的话题自带搜索意图;
2. **写作层**:创作台生成草稿时,把目标关键词交给 AI,产出后用健康分校验;
3. **收录层**:发布自动触发 IndexNow 推送 + 四搜索引擎(Google/Bing/百度/Yandex)站点工具已在后台接线。

## 软 404 与旧链接

筛选页、空页自动 noindex 并归并 canonical;旧 WordPress 链接统一 410。`SEO 中心 → 404 分诊`把日志里的 404 逐条判定为 301 / 410 / 待审。
MD],
  ['h024', 'geo-collection', 'growth', 'GEO 全网采集：让行业话题自动流进选题库', 'RSS 与搜索 API 双通道每天抓取行业信号,AI 提炼成「话题 + 切入角度 + 为什么值得写」。', false, 4, <<<MD
> GEO 解决的是**选题从哪来**:你睡觉时,系统替你盯行业信息源。

## 配置三步

1. `后台 → GEO 采集 → 源管理`:添加 RSS 源(默认示例:36氪职场、哈佛商业评论等,换成你行业的即可);
2. 开启每日采集(cron 心跳驱动,见「自部署运维清单」一篇);
3. 每天看 `话题列表`:每条话题带热度、切入角度、为什么值得写。

## 从话题到文章

选中话题 → 「生成草稿」:AI 按 SEO 结构(答案段在前、5-7 个小标题、文末 FAQ)写出 800-1200 字草稿,自动进入**待审核队列**——发布永远由人拍板。

## 前提条件

GEO 的 AI 环节依赖模型接入。`后台 → AI 配置`里至少启用一个供应商并确认 Key 有效;AI 不可用时采集照常,提炼会顺延重试。
MD],
  ['h025', 'trend-radar-daily', 'ai', '热点雷达：每天早上的 5 分钟情报站', 'HackerNews、GitHub Trending 与你的 RSS 源,去重、AI 聚类、打热度,一键转成 GEO 选题。', false, 5, <<<MD
> 热点雷达与 GEO 采集的分工:**雷达盯全球技术圈**,GEO 盯你的行业信息源。

## 每天怎么用

1. `后台 → 内容生产 → 热点雷达`,看昨日聚类出的热点话题(带热度分与来源);
2. 觉得值得写的,点「转选题」——话题带着角度直接进入 GEO 选题库,下一步就能生成草稿;
3. 不相关的忽略即可,已看过的话题会被去重,不会重复出现。

## 频率与成本

每日自动跑一次(cron 心跳驱动);AI 不可用时退化为按来源取 Top 条目,不会白屏。抓取与聚类都很轻,成本几乎可以忽略。
MD],
  ['h026', 'agent-runtime', 'ai', 'Agent 运行时与一句话指挥：受控的自主执行', '给目标而不是步骤,AI 在白名单工具里链式执行;安全动作自动做,中高风险等你批准。', true, 6, <<<MD
> OpenFlow 的 Agent 不是聊天按钮:**每个工具都有参数白名单,每个动作都有风险分级**。

## 两种用法

1. **一句话指挥**(控制台):输入「给 7 天没来的会员发一封召回邮件」,AI 产出 1-3 步可执行计划,你逐步确认;
2. **Agent 运行时**(`后台 → Studio 模式 → Agent 运行时`):输入目标,AI 自主选择工具链式执行,时间线完整展示思考、调用参数与结果。

## 风险分级

- **safe**(打标签、建任务、加积分):自动执行;
- **moderate / high**(更新线索、发券、发内容):停下等你点「批准」,批准后续跑;
- 全程落盘可审计,失败自动重试 3 次。

## 与 Flow 的关系

成熟的执行路径沉淀为 Flow;探索性的目标交给 Agent。Agent 跑通了的路径,可以固化成新 Flow——这就是经验复利。
MD],
  ['h027', 'conversion-goals-funnel', 'growth', '转化目标与漏斗：埋点、回传与归因闭环', '定义带价值的转化目标,落地页 CTA 自动绑定追踪,五阶段漏斗看清每一环流失。', false, 7, <<<MD
> 转化追踪的完整链路:**目标定义 → 埋点采集 → 归因写入 → 漏斗呈现**。

## 定义一个转化目标

`后台 → 洞察 → 转化目标`:命名目标(如「咨询表单提交」),绑定事件、作用域与价值(比如一单咨询值 ¥200)。落地页编辑器里给 CTA 指定该目标后,点击自动上报,无需写代码。

## 漏斗怎么看

`后台 → 洞察 → 转化漏斗`:访客 → 注册 → 加购 → 支付 → 复购五阶段,阶段间转化率一目了然。配合「归因」页看每个渠道对成交的真实贡献。

## 外部回传

对接了广告平台的用户可开启 CAPI 类回传(转化事件的调度与重试由系统处理),让广告平台拿到真实转化信号,模型才有得学。
MD],
  ['h028', 'deploy-ops-checklist', 'account', '自部署运维清单：心跳、备份、体检与 404 分诊', '自部署用户照着这 5 条检查,系统能力一分不少:定时心跳、备份、自我体检、404 分诊、密钥安全。', true, 8, <<<MD
> 开源版装完只是开始,这 5 条让系统「活」起来。

## 1. 定时心跳(最重要)

没有心跳,定时发布、自动化队列、热点雷达、GEO 采集都不会跑。服务器加一条:

```
* * * * * curl -fsS --max-time 55 "https://你的域名/api/cron.php?secret=你的密钥" >/dev/null 2>&1
```

密钥在 `data/cron_secret.json`。装好后在 `后台 → 系统体检` 确认定时任务已被识别。

## 2. 备份

`后台 → 系统 → 备份`:一键备份 data/ 与数据库,建议下载异地留存一份。宝塔用户可再配目录级每日备份。

## 3. 自我体检

`后台 → 系统体检`:定时任务、存储、错误日志、404 趋势六来源自动出改进清单。**体检只建议,不动手**——所有修复都等你确认。

## 4. 404 分诊

`SEO 中心 → 404 分诊`:把线上 404 逐条判成 301 / 410 / 待审,旧链接权重不流失。

## 5. 密钥安全

`data/` 目录已被 web 层规则拦截;API Key 建议定期在 `AI 配置` 里轮换。部署后第一时间改掉默认管理员密码。
MD],
  ['h029', 'publish-all-platforms', 'create', '全平台发布：内容同步到 Telegram / Discord / Mastodon / WordPress', '适配器注册表 + 统一发布队列:填好凭据、发测试消息,内容分发从「人肉复制」变成「队列自动跑」。', false, 9, <<<MD
> 发布框架的设计原则:**未就绪明确报错,不假成功**。

## 接入三步

1. `后台 → 内容分发 → 平台适配器与凭据`:选平台填凭据(Telegram Bot Token / Discord Webhook / Mastodon 实例+Token / WordPress 应用密码);
2. 点「发测试消息」验证连通;
3. 之后发布文章时勾选目标平台,或交给发布队列自动跑(失败自动重试 3 次,退避间隔递增)。

## 手动平台的诚实降级

暂无 API 的平台(如小红书)不会假装发布:系统生成原生格式文案 + 复制入口,最后一步由人完成。

## 通用 Webhook

任何自建发布器 / n8n / Make,用通用 Webhook 适配器即可接入,系统 POST 标准化内容结构给你的端点。
MD],
];

$d = json_decode((string)file_get_contents($file), true);
if (!is_array($d)) { echo "help-center.json 解析失败,中止。\n"; exit(1); }
$existing = [];
foreach (($d['articles'] ?? []) as $a) $existing[$a['slug'] ?? ''] = true;

$toAdd = [];
foreach ($NEW as $row) {
    [$id, $slug, $cat, $title, $excerpt, $hot, $order, $content] = $row;
    if ($existing[$slug] ?? false) { echo "  跳过(已存在): {$slug}\n"; continue; }
    $toAdd[] = ['id' => $id, 'slug' => $slug, 'cat' => $cat, 'title' => $title, 'excerpt' => $excerpt, 'hot' => $hot, 'order' => $order, 'updated_at' => $TODAY, 'content' => $content];
    echo "  新增: [{$cat}] {$title}\n";
}
if (!$toAdd) { echo "没有需要新增的文章。\n"; exit(0); }
echo "共 " . count($toAdd) . " 篇。\n";
if ($DRY) { echo "(dry-run,未写入)\n"; exit(0); }

$bak = $file . '.bak-' . date('YmdHis');
copy($file, $bak);
echo "已备份 → " . basename($bak) . "\n";
$d['articles'] = array_merge($d['articles'] ?? [], $toAdd);
json_write($file, $d);
echo "✅ 已写入 help-center.json(现共 " . count($d['articles']) . " 篇)\n";
